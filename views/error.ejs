<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>
      <%= typeof seo !== 'undefined' && seo && seo.title
        ? seo.title
        : 'Error | GS Infra Estates' %>
    </title>
    <meta name="description"
          content="<%= typeof seo !== 'undefined' && seo && seo.desc ? seo.desc : 'An error occurred' %>" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  </head>
  <body style="font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;">
    <% const code = typeof statusCode !== 'undefined' ? statusCode : 500; %>
    <% const msg  = typeof message !== 'undefined' ? message : 'Server error'; %>

    <div style="min-height: 100vh; display:flex; align-items:center; justify-content:center; background:#f3f4f6;">
      <div style="background:white; padding:24px 28px; border-radius:12px; box-shadow:0 10px 25px rgba(0,0,0,0.08); max-width:480px; width:100%; text-align:center;">
        <h1 style="font-size:32px; font-weight:700; margin-bottom:8px; color:#111827;"><%= code %></h1>
        <p style="font-size:15px; color:#4b5563; margin-bottom:16px;"><%= msg %></p>

        <% if (typeof stack !== 'undefined' && stack && process.env.NODE_ENV === 'development') { %>
          <pre style="text-align:left; white-space:pre-wrap; font-size:12px; color:#6b7280; background:#f9fafb; padding:12px; border-radius:8px; max-height:260px; overflow:auto;">
<%= stack %>
          </pre>
        <% } %>

        <a href="/" style="display:inline-block; margin-top:16px; padding:8px 16px; border-radius:9999px; background:#2563eb; color:white; text-decoration:none; font-size:14px;">
          Go to Home
        </a>
      </div>
    </div>
  </body>
</html>
